// let fn = (...spaces) => (spaces.split(' ').length - 1);
// fn('dev Mentor', 'devMentor', ' ');
let fn = (...args) => (
    let result = 0
for (let i = 0; i < string.length; i++) {
    result = args.split
)
